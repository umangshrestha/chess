import type { Meta } from "@storybook/react";
import Footer from "../components/Footer";

const meta = {
  title: "Footer",
  component: Footer,
} as Meta;

export default meta;

export const Default = {};
